# FRP 公共配置
# 服务端和客户端共享的配置

server:
  addr: "115.190.198.49"
  port: 443

auth:
  method: "token"
  token: "llm-gateway-secret-token-2026"

transport:
  tls_enable: true
  pool_count: 5

log:
  level: "info"
  max_days: 3

# 下载配置
download:
  version: "0.66.0"
  proxy: "http://127.0.0.1:1082"
  # 镜像源（可选，留空使用 GitHub）
  mirror: ""

